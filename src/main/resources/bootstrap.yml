spring:
  application:
    name: microservice-commandes # Nom de votre application
  cloud:
    config:
      uri: http://localhost:8882 # Adresse de votre Config Server
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: refresh
